import { createAction } from '@ngrx/store';

export const selectAgence = createAction('SELECT_AGENCE');
export const inputClient = createAction('INPUT_CLIENT');
export const inputLieu = createAction('INPUT_LIEU');
export const selectDate = createAction('SELECT_DATE');

